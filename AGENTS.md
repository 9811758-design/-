# 카페 장부 PWA 작업 규칙

## 모든 프롬프트와 응답의 첫 줄

반드시 다음 형식으로 작업 목적을 먼저 밝힌다.

`목적: [이번 작업으로 사용자에게 제공할 결과]`

예: `목적: 판매 거래를 오프라인에서도 안전하게 저장할 수 있는 로컬 데이터 계층을 만든다.`

## 작업 절차

1. `README.md`와 관련 설계 문서를 먼저 읽는다.
2. 이번 단계의 완료 조건과 영향을 받는 문서를 확인한다.
3. 작고 빌드 가능한 단위로 구현한다.
4. 구현 전제나 제품 결정이 바뀌면 `docs/decisions.md`에 기록한다.
5. 관련 테스트, 타입 검사, 린트, 빌드를 실행한다.
6. 완료 보고에는 변경 파일, 검증 결과, 남은 위험만 간결히 적는다.

## 지켜야 할 제품 원칙

- 모바일에서 빠르게 입력할 수 있어야 한다.
- 금액은 부동소수점이 아니라 정수 원 단위로 저장·계산한다.
- 거래에는 UUID, 생성·수정 시각, 동기화 상태, 삭제 시각을 둔다.
- 로컬 데이터베이스가 작업 중의 기준 데이터다. 동기화 실패가 입력 데이터를 잃게 해서는 안 된다.
- 삭제는 기본적으로 soft delete이며, Google Sheets에도 삭제 상태가 반영되어야 한다.
- Google OAuth 비밀값이나 API 키를 코드에 넣지 않는다.
- 화면만 만들고 기능을 구현했다고 주장하지 않는다.
- MVP 밖 기능을 미리 과도하게 구현하지 않는다.

## 변경 전 확인이 필요한 결정

다음은 임의로 확정하지 말고 `docs/decisions.md`에 제안·근거를 남긴 뒤 사용자 확인을 받는다.

- VAT 포함/별도 방식
- 결제 수단 구분 범위
- 여러 기기 동시 사용 정책
- 재료 단가 방식(최근가, 이동평균, 가중평균)
- 환불·매출 취소 방식
- Google Sheets를 새로 만들지, 기존 문서를 연결할지

## 완료 선언 기준

기능별 정상 흐름과 실패 흐름을 모두 검증하고, 앱이 빌드되는 경우에만 완료로 보고한다. 검증하지 못한 항목은 완료라고 하지 않고 이유를 남긴다.
